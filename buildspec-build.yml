version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Building the application...
      - npm run build || npm run start
artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: .
